public class mathmethods {
    public static void main(String[] args) {
        System.out.println(Math.max(10, 20));
        System.out.println(Math.min(10, 20));
        System.out.println(Math.sqrt(25));
        System.out.println(Math.abs(-7));// absolute value of pos no.is that no. itself
                                         // neg no.will be positive of that no
        System.out.println(Math.random());
        // this will give output in a float value.to convertthis into int we can do next
        // step
        System.out.println((int) (Math.random() * 100));// here we will get a random no.chosen by computer
        // that no.will be btw 0 and 100

    }
}
